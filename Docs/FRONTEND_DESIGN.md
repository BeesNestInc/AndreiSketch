- Svelteコンポーネント内で入力されたSQLを、`axios.post`を使いサーバーへ送信し、結果をリアクティブに表示する。
- 結果データ(JSON)は、まず基本的なHTMLの`<table>`を用いて画面にレンダリングする。
- 将来的には、`Observable Plot`などの可視化ライブラリを利用し、結果セットをチャートとして表示する機能を追加することを想定する。
- セッション管理は不要。オンプレミス環境との接続検証と、クエリの試行錯誤に特化する。
